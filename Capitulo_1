/*
//pedir notas e imprimir el promedio
#include <stdio.h>
#include <stdlib.h>


int main()
{
    int notas[5],i;
    int nota=0;
    for (i=0;i<5;i++)
    {
        printf("\nIntroduzca la nota %d : ",i+1);
        scanf("%d",&notas[i]);
        nota+=notas[i];
    }
    printf("El promedio de las notas ingresadas es: %d",nota/5);
    return 0;
}
*/
/*

//imprime el mayor de un arreglo
#include <stdio.h>
#include <stdlib.h>


int main()
{
    int arreglo[10],mayor,i,j;

    for (i=0;i<10;i++)
    {
        printf("Introduzca el valor %d: ",i+1);
        scanf("%d",&arreglo[i]);
        printf("\n");
    }
    mayor=arreglo[0];
    for (j=0;j<9;j++)
    {
        if (mayor<arreglo[j+1])
            mayor=arreglo[j+1];
    }
    printf("El numero mayor es: %d",mayor);
}
*/
/*

//imprime el menor de un arreglo
#include <stdio.h>
#include <stdlib.h>


int main()
{
    int arreglo[10]={3,5,6,1,8,4,2,7,5,9};
    int menor=0,i;
    for (i=0;i<10;i++)
    {
        if (arreglo[i]<arreglo[menor])
            menor=i;
    }
    printf("El arreglo mayor es: %d",arreglo[menor]);
}
*/

/*
//imprime un programa que imprima el histograma de las longitudes de las palabras
#include <stdio.h>
#include <stdlib.h>


int main()
{
    int arreglo[20];
    char c;
    int i,j,cont=0;
    for (i=0;i<20;i++)
        arreglo[i]=0;
    while((c=getchar())!=EOF)
    {
        if (c==' ')
            cont++;
        else
            arreglo[cont]++;
    }
    for(i=0;i<20;i++)
    {
        for (j=0;j<arreglo[i];j++)
        {
            printf("*");
        }
        printf("\n");
    }
}
*/


